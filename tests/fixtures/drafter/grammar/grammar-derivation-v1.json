{
  "id": "grammar-derivation-v1",
  "kind": "grammar-derivation-contract",
  "cases": [
    {
      "name": "baseline",
      "metadata": [
        {"kind": "use_item", "canonical_start": "use", "operand_names": ["item"], "category": "Policy"},
        {"kind": "clear_state", "canonical_start": "clear state", "operand_names": [], "category": "Administrative"}
      ],
      "canonical_starts": ["clear state", "use"],
      "prompt_forms": [
        "- `use <item>` (Policy)",
        "- `clear state` (Administrative)"
      ],
      "incomplete": {"use": true, "adopt": false},
      "rewrite": {"kind": "use_item", "operands": ["docker"], "expected": "use docker"}
    },
    {
      "name": "added_directive",
      "metadata": [
        {"kind": "use_item", "canonical_start": "use", "operand_names": ["item"], "category": "Policy"},
        {"kind": "clear_state", "canonical_start": "clear state", "operand_names": [], "category": "Administrative"},
        {"kind": "adopt_rule", "canonical_start": "adopt", "operand_names": ["rule"], "category": "Policy"}
      ],
      "canonical_starts": ["clear state", "adopt", "use"],
      "prompt_forms": [
        "- `use <item>` (Policy)",
        "- `clear state` (Administrative)",
        "- `adopt <rule>` (Policy)"
      ],
      "incomplete": {"use": true, "adopt": true},
      "rewrite": {"kind": "adopt_rule", "operands": ["strictness"], "expected": "adopt strictness"}
    }
  ]
}
